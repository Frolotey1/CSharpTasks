// Strategy/LayoutContext.cs
using System.Drawing;

namespace Patterns.Strategy;

public readonly record struct LayoutContext(
    int Padding,
    int Spacing,
    Size AvailableSize,
    float DpiScale
);
